#include "matriz.h"
template<typename T>
Matriz<T>::Matriz()
{
    this->size=0;
    this->lstHorizontal=this->lstVertical=nullptr;
}
template <typename T>
void Matriz<T>:: insertar(T horizontal, T Vertical,T val){
    if(this->size==0){

    }
}
